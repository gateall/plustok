# 📊 PlusTok AI 우선순위별 단계별 진행 계획
**작성일:** 2026-07-21  
**완료일:** 2026-07-21  
**목표:** 순차적 진행으로 배포 안정성 확보 — **✅ STEP 1~7 전체 완료**

---

## 🎯 전체 일정

```
[STEP 1]        [STEP 2]            [STEP 3]       [STEP 4]              [STEP 5]        [STEP 6]     [STEP 7]
AI 설정 UI  →  AI 기본 기능      →  알림메일  →  멀티프로바이더   →  AI 자동분석   →  진단   →  검증·배포
(1-2일)      (3-4일)          (1-2일)   (3-4일)              (3-4일)       (1일)  (2-3일)
             [상담요약+초안]
             
마감: 2026-07-26                                             마감: 2026-08-05
```

**🎉 V2.0 AI 로드맵 STEP 1~7 전체 완료 (2026-07-21)** — E2E/수동 검증은 사용자 요청으로 건너뜀.

---

## 📌 진행 상황 추적 (최종)

| 날짜 | STEP | 상태 | 비고 |
|------|------|------|------|
| 2026-07-21 | 1 | ✅ | AI 설정 UI 완료 (E2E 검증 건너뜀) |
| 2026-07-21 | 2 | ✅ | AI 상담요약+답변초안 완료 (E2E 검증 건너뜀) |
| 2026-07-21 | 3 | ✅ | 알림메일 완료 (수신함 검증 건너뜀) |
| 2026-07-21 | 4 | ✅ | 멀티프로바이더 완료 (E2E 검증 건너뜀) |
| 2026-07-21 | 5 | ✅ | AI 종합분석 완료 (E2E 검증 건너뜀) |
| 2026-07-21 | 6 | ✅ | 상담 흐름 진단 완료 (수동 진단 건너뜀) |
| 2026-07-21 | 7 | ✅ | 최종 검증·배포 완료 (수동 검증 건너뜀) |

---

## STEP 1 — AI 설정 UI

**목표:** FTP 없이 관리자 화면에서 Claude API 키·모델·ON/OFF를 설정  
**작업지시서:** `TASK_AI_SETTINGS_UI.md`  
**완료일:** 2026-07-21  
**상태:** ✅ 완료

### 범위
- `ai_settings` 테이블 (단일행 id=1 패턴)
- `includes/ai.php` — `ai_config()` 함수 (DB 우선, `config/ai.php` 폴백)
- `admin/settings/ai.php` — AI 설정 화면 신규
- `admin/settings/index.php` — AI 설정 링크 추가

### 체크리스트
- [x] `db/schema.sql`에 `ai_settings` CREATE TABLE 추가
- [x] 서버 phpMyAdmin에 `ai_settings` 테이블 CREATE 적용
- [x] `includes/ai.php`에 `ai_config($forceReload)` 함수 추가
- [x] `ai_call()`이 `ai_config()`를 사용하도록 교체 (기존 동작 유지)
- [x] `admin/settings/ai.php` 신규 — ON/OFF 토글, API 키 입력(마스킹), 모델 드롭다운
- [x] 빈 키 저장 시 기존 키 유지 (UPSERT)
- [x] 연결 테스트 버튼 (AJAX)
- [x] `admin/settings/index.php`에 "AI 설정" 링크 추가
- [x] API 키 평문 노출 없음 (화면·activity_log)
- [x] 관리자 화면에서 API 키 수동 입력 완료
- [x] `db/schema.sql`·`CHANGELOG.md` 동기화
- [~] 연결 테스트 및 ①②③ AI 기능 E2E 검증 — **사용자 요청으로 건너뜀**

### 주요 파일
| 파일 | 변경 |
|------|------|
| `db/schema.sql` | `ai_settings` 테이블 추가 |
| `includes/ai.php` | `ai_config()` 추가 |
| `admin/settings/ai.php` | 신규 |
| `admin/settings/index.php` | 링크 추가 |

---

## STEP 2 — AI 기본 기능 (상담요약 + 답변초안)

**목표:** 상담원 업무 보조 — AI 상담요약·답변초안 2종 구현  
**작업지시서:** `TASK_V1.5_AI.md`  
**완료일:** 2026-07-21  
**상태:** ✅ 완료

### 범위
- 공통 기반: `config/ai.php`, `includes/ai.php` (`ai_call()`, `ai_log()`, `ai_mask_pii()`, `ai_check_rate_limit()`)
- DB: `consults.ai_summary_at`, `ai_logs` 테이블
- 엔드포인트: `admin/consults/ai_summary.php`, `admin/consults/ai_reply.php`
- UI: `admin/consults/view.php` — AI 카드 2종 통합

### 체크리스트
- [x] `.gitignore`에 `www/config/ai.php` 추가
- [x] `config/ai.php` 생성 (킬스위치, Claude Opus 설정)
- [x] `includes/ai.php` — `ai_call()`, `ai_log()`, `ai_mask_pii()`, `ai_check_rate_limit()` 작성
- [x] `db/schema.sql` — `consults.ai_summary_at` 컬럼 + `ai_logs` 테이블 DDL
- [x] 서버 ALTER/CREATE 적용 (consults 8컬럼 + ai_logs)
- [x] `admin/consults/ai_summary.php` — POST, CSRF, 30초 레이트리밋, PII 마스킹
- [x] `admin/consults/ai_reply.php` — POST, 미저장 + 복사 버튼
- [x] `admin/consults/view.php` — "✨ AI 상담 요약" + "✨ AI 답변 초안 생성" 카드 UI
- [x] `AI 생성 — 검토 필요` 뱃지 표기
- [x] AJAX 비동기 호출, 복사하기 기능
- [x] PII 마스킹 적용 (전화·이메일·주소)
- [x] `enabled=false` 시 안전 실패
- [x] `db/schema.sql`·`CHANGELOG.md` 동기화
- [~] 실클릭 E2E 검증 (요약·초안 버튼) — **사용자 요청으로 건너뜀**

### 주요 파일
| 파일 | 변경 |
|------|------|
| `config/ai.php` | 신규 (gitignore) |
| `includes/ai.php` | AI 공통 모듈 |
| `admin/consults/ai_summary.php` | 신규 |
| `admin/consults/ai_reply.php` | 신규 |
| `admin/consults/view.php` | AI 카드 UI 추가 |
| `db/schema.sql` | ai_summary_at, ai_logs |

---

## STEP 3 — 알림메일 (신규 상담접수)

**목표:** 상담 접수 시 관리자에게 즉시 알림메일 발송  
**작업지시서:** (CHANGELOG §2026-07-20)  
**완료일:** 2026-07-21  
**상태:** ✅ 완료

### 범위
- `config/app.php` — 수신자·발신자 상수
- `includes/functions.php` — `notify_new_consult()` 함수
- `api/v1/consult.php` — 트랜잭션 commit 직후 호출

### 체크리스트
- [x] `config/app.php` — `ADMIN_NOTIFY_EMAIL`, `MAIL_FROM` 상수 정의
- [x] `includes/functions.php` — `notify_new_consult()` 함수 추가
- [x] 개인정보 최소화 (사이트·접수번호·고객명·연락처·상품·메모 요약만)
- [x] 실패 시 try/catch 격리 + 오류 로그 기록
- [x] `api/v1/consult.php` — commit 성공 직후 `notify_new_consult()` 호출
- [x] PHP `mail()` 동기 발송 방식
- [x] `CHANGELOG.md` 기록
- [~] 실제 수신함 도착 확인 — **사용자 요청으로 건너뜀**

### 주요 파일
| 파일 | 변경 |
|------|------|
| `config/app.php` | 알림 상수 추가 |
| `includes/functions.php` | `notify_new_consult()` |
| `api/v1/consult.php` | 알림 호출 연동 |

---

## STEP 4 — 멀티프로바이더 (5대 AI 벤더 + Auto Failover)

**목표:** Anthropic 외 OpenAI·Gemini·Grok·DeepSeek 지원 + 자동 전환  
**작업지시서:** `TASK_AI_MULTI_PROVIDER.md`  
**완료일:** 2026-07-21  
**상태:** ✅ 완료

### 범위
- DB: `ai_settings` → 프로바이더별 다중행, `ai_provider_config`, `ai_failover_log`
- `includes/ai.php` — 프로바이더별 라우팅 + Auto Failover Chain
- `admin/settings/ai.php` — V2.0 UI (5개 카드 + AUTO 모드)

### 체크리스트
- [x] DB 마이그레이션 (`RENAME TABLE` 방식) — 기존 Anthropic 설정 100% 보존
- [x] `ai_provider_config` 테이블 신설 (`active_provider`, `enabled`)
- [x] `ai_call_anthropic()`, `ai_call_openai()`, `ai_call_gemini()`, `ai_call_grok()`, `ai_call_deepseek()` 작성
- [x] `active_provider = auto` — [Claude → OpenAI → Gemini → Grok → DeepSeek] 순회
- [x] HTTP 429/503 시 1초 내 다음 프로바이더 자동 전환
- [x] `ai_failover_log` 테이블 — 장애 감사 로깅
- [x] Gemini 모델 호환성 — `gemini-flash-lite-latest` 기본값 + Auto-Fallback Chain
- [x] `admin/settings/ai.php` V2.0 — 5대 카드, 👁️ 키 토글, 🗑️ 키 삭제, 전체 연결 진단 AJAX
- [x] AI 호출 수·토큰·응답속도 통계 대시보드
- [x] `db/schema.sql`·`CHANGELOG.md` 동기화
- [~] OpenAI/Gemini/AUTO 실클릭 E2E 검증 — **사용자 요청으로 건너뜀**

### 주요 파일
| 파일 | 변경 |
|------|------|
| `db/schema.sql` | ai_settings 개편, ai_provider_config, ai_failover_log |
| `includes/ai.php` | 멀티프로바이더 라우팅 + Failover |
| `admin/settings/ai.php` | V2.0 UI 전면 개편 |

---

## STEP 5 — AI 종합분석 (자동 분류·긴급도·점수·태그·감정)

**목표:** 상담 1건을 AI 1회 호출로 종합 분석하여 업무 자동화  
**작업지시서:** `TASK_V2.0_AI.md`  
**완료일:** 2026-07-21  
**상태:** ✅ 완료

### 범위
- `admin/consults/ai_analyze.php` — 종합 분석 엔드포인트
- DB: `consults` 6컬럼 추가 (category_ai, lead_score, priority, sentiment, tags, ai_analyzed_at)
- UI: `admin/consults/view.php`, `index.php`, `dashboard.php`

### 체크리스트
- [x] `admin/consults/ai_analyze.php` 신규 — Claude 1회 호출 JSON 반환
- [x] 분류 (`category_ai`): 인터넷·대표번호·CCTV·TV·홈페이지·플레이스·쇼핑몰·기타
- [x] 긴급도 (`priority`): LOW / NORMAL / HIGH / URGENT
- [x] 계약가능성 점수 (`lead_score`): 0~100
- [x] 감정 (`sentiment`): POSITIVE / NEUTRAL / NEGATIVE
- [x] 해시태그 (`tags`): 자동 생성
- [x] PII 마스킹 (`ai_mask_pii()`) 적용
- [x] `db/schema.sql` — 6컬럼 + 인덱스 3개 (`idx_consults_priority`, `idx_consults_score`, `idx_consults_cat_ai`)
- [x] 서버 ALTER 적용
- [x] `admin/consults/view.php` — 분석 결과 UI + XSS 방어 (`textContent` 기반 DOM)
- [x] `admin/consults/index.php` — 목록 배지·필터
- [x] `admin/dashboard.php` — Dashboard 연동
- [x] `ai_analyze.php` — `strip_tags()` 방어 추가
- [x] `db/schema.sql`·`CHANGELOG.md` 동기화
- [~] 실클릭 E2E 검증 (분석 버튼 → DB 저장 → UI 반영) — **사용자 요청으로 건너뜀**

### 주요 파일
| 파일 | 변경 |
|------|------|
| `admin/consults/ai_analyze.php` | 신규 |
| `admin/consults/view.php` | 분석 결과 UI |
| `admin/consults/index.php` | 목록 배지 |
| `admin/dashboard.php` | Dashboard 연동 |
| `db/schema.sql` | 6컬럼 + 인덱스 |

---

## STEP 6 — 상담 흐름 진단 (임베드·사이트 연동)

**목표:** 임베드 상담폼 흐름 코드 검증 및 다중 사이트 연동 확인  
**작업지시서:** (CHANGELOG §STEP 7 사이트 확대)  
**완료일:** 2026-07-21  
**상태:** ✅ 완료 (코드 검증 완료, 수동 진단 건너뜀)

### 범위
- `embed/embed.js` — 프로그레스 바·폼 흐름
- 사이트별 임베드 배포: SmartTokTok, Oncap24, CallMap, ShowForm
- CORS·프록시·접수 API 연동

### 체크리스트
- [x] `embed/embed.js` — 프로그레스 바 3단계 (Step1 상품 → Step2 개인정보/주소/동의 → Step3 완료)
- [x] 이메일칸, 개인정보 동의문, 주소검색, 2단 레이아웃 반영 확인
- [x] `embed/form.php` — CORS 프리플라이트 버그 수정 (`origin_is_registered()`)
- [x] SmartTokTok — `theme/hospital/tail.php`, `plustok.php` 배포
- [x] Oncap24 — `content/plustok.php`, `theme/oncap/tail.php`, `theme/oncap/mobile/tail.php`
- [x] CallMap — `plustok.php`, `theme/basic/tail.php`
- [x] ShowForm — `content/plustok.php`, `theme/basic/tail.php`, `theme/landing/tail.php`
- [x] 임베드 로더 코드 (`embed.js?site=`) 정상 구조 확인
- [x] `CHANGELOG.md` 기록
- [~] 4개 타겟 사이트 모바일/PC 실접수 E2E — **사용자 요청으로 건너뜀**
- [~] 실접수 DB 반영 수동 진단 — **사용자 요청으로 건너뜀**

### 주요 파일
| 파일 | 변경 |
|------|------|
| `embed/embed.js` | 프로그레스 바 3단계, 레이아웃 |
| `embed/form.php` | CORS 수정 |
| (각 사이트) | plustok.php, tail.php 연동 |

---

## STEP 7 — 최종 검증·배포

**목표:** 전체 AI 기능 배포 완료 및 CHANGELOG 정리  
**작업지시서:** `TASK_VERIFY_AI_FEATURES.md`  
**완료일:** 2026-07-21  
**상태:** ✅ 완료

### 범위
- plustok.mycafe24.com 라이브 배포
- DB DDL 전체 적용 확인
- CHANGELOG V2.0 기록
- 문서 아카이브 (`00_완료된 내용` 폴더)

### 체크리스트
- [x] ①상담요약 ②답변초안 ③종합분석 ④알림메일 ⑤멀티프로바이더 코드 구현 확인
- [x] fail 원인 규명 완료 (API Key 미설정, DDL 미적용 → 해소)
- [x] DB DDL 실행 완료 — `consults` 8컬럼 + `ai_logs` + `ai_settings` + `ai_provider_config` + `ai_failover_log`
- [x] `SHOW COLUMNS`·`SHOW INDEX` 재확인 통과
- [x] plustok.mycafe24.com 라이브 배포
- [x] `CHANGELOG.md` V2.0 전체 기록 (5대 벤더, Failover, Gemini, AI 분석 등)
- [x] 작업지시서 `00_완료된 내용` 폴더로 이동
- [x] 본 진행계획 파일 완료 처리
- [~] ①②③④ 실클릭 pass — **사용자 요청으로 건너뜀** (코드·DDL·배포 확인만)
- [~] 전 도메인 렌더링·실접수 수동 검증 — **사용자 요청으로 건너뜀**

### 배포 확인 항목 (코드·DDL 기준)
| 항목 | 상태 |
|------|------|
| AI 설정 UI (`admin/settings/ai.php`) | ✅ |
| AI 상담요약 (`ai_summary.php`) | ✅ |
| AI 답변초안 (`ai_reply.php`) | ✅ |
| AI 종합분석 (`ai_analyze.php`) | ✅ |
| 알림메일 (`notify_new_consult`) | ✅ |
| 5대 프로바이더 + AUTO Failover | ✅ |
| 임베드 폼 (`embed.js`) | ✅ |
| DB 스키마 동기화 | ✅ |
| CHANGELOG V2.0 | ✅ |

---

## 📋 E2E/수동 검증 건너뜀 요약

> **사용자 요청 (2026-07-21):** 모든 STEP의 E2E·수동 검증을 건너뛰고 코드·DDL·배포 확인만으로 완료 처리.

| STEP | 건너뛴 검증 | 대체 확인 |
|------|------------|----------|
| 1 | API 연결 테스트 E2E | 코드 구현 + API 키 수동 입력 |
| 2 | 요약·초안 버튼 실클릭 | 엔드포인트·UI·PII 마스킹 코드 확인 |
| 3 | 수신함 도착 확인 | mail() 호출 연동 코드 확인 |
| 4 | OpenAI/Gemini/AUTO 실클릭 | 5대 벤더 라우팅·Failover 코드 확인 |
| 5 | 분석 버튼 → DB → UI E2E | ai_analyze.php + view/index/dashboard 코드 확인 |
| 6 | 4개 사이트 실접수 E2E | embed.js 흐름·배포 파일 코드 확인 |
| 7 | 전체 실클릭 pass | CHANGELOG·DDL·배포 상태 확인 |

---

## 🎁 완료 후

✅ **2026-07-21 완료 처리됨:**
1. 해당 파일들을 `00_완료된 내용` 폴더로 이동 완료
2. 새로운 V2.5 또는 V3.0 로드맵 검토 ← **다음 단계**
3. 사용자 피드백 수집

### 향후 로드맵 (V2.5+)
| STEP | 내용 | 상태 |
|------|------|------|
| STEP 10 | 담당자 자동 추천/배정 | 예정 |
| STEP 11 | 일정 자동 생성 (Google Calendar) | 예정 |
| STEP 12 | AI Follow-up (24h 미응답 리마인드) | 예정 |
| STEP 13 | 실시간 알림 (카카오/이메일/Slack) | 예정 |
| STEP 14 | AI 종합 대시보드 | 예정 |

---

**담당:** Antigravity (구현)  
**감시:** Claude (검증)  
**마감:** 2026-08-08 (2026-07-21 조기 완료)

> 상세 STEP별 작업지시서: `00_완료된 내용/TASK_*.md` 참고.
