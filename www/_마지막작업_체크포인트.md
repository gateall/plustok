# 마지막 작업 체크포인트

**저장일:** 2026-07-21  
**프로젝트:** PlusTok ACEP V3.0  
**경로:** `E:\0000000_AI Enterprise Framework\PROJECTS\000_PLUS톡\www\`  
**Git commit:** 없음 (로컬 파일만 저장)  
**Cafe24 프로덕션:** Phase 3 FTP/DB 마이그레이션 미완

---

## 재개 방법

다음 세션에서 아래 중 하나로 요청:

- `Phase 3 이어서`
- `Phase 3 P2 Dashboard Failover 위젯`
- `Phase 3 배포 체크리스트부터`
- `마지막 작업 불러와`

---

## BUG 현황

| ID | 상태 | 비고 |
|----|------|------|
| BUG-001 | ✅ 완료 | `/api/v1/health` 200 |
| BUG-002 | ✅ 완료 | Frontend dist + `/frontend/` basename |
| BUG-003 | ⏸ 보류 | 웹호스팅 Node 불가 → **Cafe24 Node.js 호스팅(Git/web.js)** 또는 VPS 검토 |
| BUG-004 | ⏳ 미확인 | 프로덕션 DB 마이그레이션 |

### BUG-003 확정 사항

**웹호스팅 (2026-07-21):** SSH 가능, Node/PM2 불가, `:3001` 차단

**Node.js 호스팅 (2026-07-22 — Cafe24 권장 상품):**
- 단독 컨테이너, 웹 콘솔 앱 시작/중지, Git 배포 (FTP ❌)
- Node **v14 / v12** (v14 선택 필수 — redis v4)
- 진입점 `web.js`, 포트 `process.env.PORT`
- 도메인 `*.cafe24app.com` → Frontend `VITE_WS_URL` 변경 필요
- 상품: https://hosting.cafe24.com/?controller=new_product_page&page=language
- 문서: `_BUG003_WebSocket_진단보고서.md` Option D

---

## Phase 3 완료 (코드)

### Step 1 — CRM 통합 ✅

- `CrmCloseService`, `CrmAiPipeline`
- `ConsultRepository`, `ScheduleRepository`, `CustomerBridgeRepository`
- `POST /api/v1/consults/close`, retry, `GET /consults/{consultNo}`
- `ChatService::closeRoom()` → CRM 자동 연동
- Migration: `migrations/V3.0.1__phase3_crm.sql`, `phase3_chat_rooms.php`

### Step 2 — Admin API ✅ (대부분)

| Route | 상태 |
|-------|------|
| `/admin/stats/*` | ✅ |
| `/admin/consults` | ✅ |
| `/admin/monitor/rooms`, `…/insight` | ✅ |
| `/admin/agents` CRUD + assign | ✅ |
| `/admin/agents/…/assignments` DELETE | ✅ (P1) |
| `/admin/prompts` CRUD | ✅ (P1) |
| `/admin/failover-logs` | ✅ (P1) |
| `/admin/settings`, `/admin/audit-logs` | ✅ |
| `/admin/stats/export` | ❌ |
| `/admin/monitor/rooms/{id}/messages` | ❌ |

### Step 3 — Admin Dashboard React ⚠️ (MVP)

- Route: `/frontend/admin/dashboard` (`AdminRoute`: admin/operator)
- 파일: `frontend/src/pages/AdminDashboardPage.tsx`, `components/Admin/AdminDashboard.tsx`
- 섹션: 실시간 KPI, 상담원, AI 성과, 고객 분석, 시간대 추이, Live Monitor
- WS: `useAdminSocket` (chat-server 보류로 실시간 제한)

### P1 세션 추가 (2026-07-21)

- `includes/services/AdminPromptService.php`
- `includes/services/AdminFailoverService.php`
- `tests/Feature/AdminPromptFailoverTest.php`
- `scripts/validate_production.php` — Phase 3 CRM 테이블 검증 추가
- `_Phase3_배포체크리스트.md`

---

## 미완 / 다음 작업

### P0 — Operator (Cafe24)

1. DB 마이그레이션 (`V3.0.1` + chat_rooms ALTER)
2. FTP 업로드 → `_Phase3_배포체크리스트.md` B섹션
3. `php scripts/validate_production.php`
4. Admin API 스모크 (`/admin/stats/overview`, `/admin/prompts`)

### P2 — Dashboard SSOT 정합

- Failover Top 5 위젯 (`/admin/failover-logs` API 준비됨)
- KPI 4종 SSOT 맞추기 (대기고객, 평균해결 등)
- Sparkline, Chart 고도화

### P3 — Admin SPA 확장

- Consult List 페이지 (`/admin/consults`)
- Agent Management 페이지
- Settings / Audit / Prompt UI

### P4 — 인프라

- chat-server 별도 Node 호스팅 + `VITE_WS_URL` 재빌드

---

## 핵심 파일 목록

```
api/v1/bootstrap.php
api/v1/router.php
includes/services/CrmCloseService.php
includes/services/AdminStatsService.php
includes/services/AdminPromptService.php      ← P1
includes/services/AdminFailoverService.php    ← P1
frontend/src/components/Admin/AdminDashboard.tsx
frontend/src/pages/AdminDashboardPage.tsx
migrations/V3.0.1__phase3_crm.sql
_Phase3_배포체크리스트.md
_BUG003_WebSocket_진단보고서.md
_마지막작업_체크포인트.md                     ← 본 파일
```

---

## 프로덕션 URL

- Site: https://plustok.mycafe24.com
- Frontend: https://plustok.mycafe24.com/frontend/login
- API health: https://plustok.mycafe24.com/api/v1/health

---

*End of checkpoint*
