# 03 — 설정 관리

> **PlusTok ACEP** · STEP 6 · System / AI / Chat / CRM Settings  
> **버전**: 1.0.0 · **작성일**: 2026-07-21  
> **참조 구현**: [admin/settings/ai.php](../admin/settings/ai.php)  
> **RBAC**: [04_Admin_API_및_권한_명세.md](./04_Admin_API_및_권한_명세.md)

---

## 목차

1. [목적 (Purpose)](#1-목적-purpose)
2. [범위 (Scope)](#2-범위-scope)
3. [요구사항 (Requirements)](#3-요구사항-requirements)
4. [AI 설정](#4-ai-설정)
5. [채팅 설정](#5-채팅-설정)
6. [CRM 설정](#6-crm-설정)
7. [알림 설정](#7-알림-설정)
8. [Business Rule·권한](#8-business-rule권한)
9. [API·Storage](#9-apistorage)
10. [Test Case](#10-test-case)
11. [Future](#11-future)

---

## 1. 목적 (Purpose)

시스템 전역 설정(AI·채팅·CRM·알림)을 Admin 콘솔에서 중앙 관리한다.  
기존 `ai.php` 멀티 프로바이더 UI를 SSOT로 문서화하고 ACEP 확장 설정을 정의한다.

---

## 2. 범위 (Scope)

| Module | Path | Roles |
|--------|------|-------|
| AI | /admin/settings/ai.php | super, admin |
| Prompts | /admin/settings/prompts.php | super, admin |
| Failover log | /admin/settings/failover.php | + operator read |
| General | /admin/settings/general.php | super, admin subset |
| CRM rules | /admin/settings/crm.php | super, admin |

---

## 3. 요구사항 (Requirements)

| ID | 요구사항 |
|----|----------|
| REQ-CFG-001 | AI key encrypted at rest |
| REQ-CFG-002 | super only key reveal |
| REQ-CFG-003 | settings change → audit_logs |
| REQ-CFG-004 | test connection rate limit 5/min |

---

## 4. AI 설정

### 4.1 ai.php 구조 (참조 구현)

| Section | Fields |
|---------|--------|
| Global kill switch | ai_provider_config.enabled |
| Active provider | anthropic, openai, gemini, grok, deepseek, **auto** |
| Per-provider | api_key, model, temperature, max_tokens, timeout |
| Failover | auto chain order ([04_AI/01_AI전략.md](../04_AI/01_AI전략.md)) |

### 4.2 AJAX Actions (ai.php)

| _action | Function |
|---------|----------|
| test | ai_call() active provider |
| test_provider | ai_call_single_provider() |
| test_all | 5 providers loop |
| set_active_provider | DB UPDATE active_provider |
| save_provider | UPSERT keys/models |

### 4.3 Prompt 버전

- Table: `ai_prompts`
- UI: `/admin/settings/prompts.php`
- Rule BR-CFG-020: admin cannot delete prod prompt

### 4.4 ASCII

```
┌─────────────────────────────────────────────────────────────────────────────┐
│  AI 설정                                                                     │
│  Active: (•) Auto  ( ) Anthropic  ( ) OpenAI  ...                           │
│  [x] AI 전역 활성화                                                          │
│  Per-provider cards: API Key [masked] Model Temperature [Test] [Save]       │
│                              [전체 연결 테스트]  [저장]                        │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 5. 채팅 설정

| Key | Default | Description |
|-----|---------|-------------|
| chat.max_concurrent_rooms | 500 | platform-wide |
| chat.message_retention_days | 365 | purge job |
| chat.auto_close_idle_min | 30 | 무응답 종료 |
| chat.max_file_mb | 10 | UPLOAD_MAX_BYTES |
| websocket.url | wss://... | super only |

Storage: `settings` KV or `system_config` JSON.

---

## 6. CRM 설정

| Key | Default | Description |
|-----|---------|-------------|
| crm.auto_save_enabled | true | close → CRM |
| crm.min_duration_sec | 300 | BR-CRM-001 |
| crm.min_messages | 3 | BR-CRM-002 |
| crm.retry_max | 3 | |
| crm.schedule.high_score_days | 3 | lead≥70 |
| crm.schedule.mid_score_days | 7 | 50-69 |
| crm.schedule.low_score_days | 30 | <50 |
| crm.email_template.{category} | TEXT | follow-up draft |

Ref: [06_CRM/01_CRM통합.md](../06_CRM/01_CRM통합.md) §7

---

## 7. 알림 설정

| Channel | Events | Recipients |
|---------|--------|------------|
| Admin email | new consult, CRM fail, failover spike | ADMIN_NOTIFY_EMAIL |
| Agent toast | assignment, CRM save fail | agent app |
| Customer | — | V4.0 (opt-in) |

config/app.php: `ADMIN_NOTIFY_EMAIL`, `MAIL_FROM`

---

## 8. Business Rule·권한

| Rule ID | Rule |
|---------|------|
| BR-CFG-001 | super only API key plaintext |
| BR-CFG-002 | admin can edit temperature, not keys |
| BR-CFG-003 | operator no settings access |
| BR-CFG-004 | maintenance mode super only |
| BR-CFG-005 | ai test 5/min/user |

---

## 9. API·Storage

### 9.1 Tables

| Table | Usage |
|-------|-------|
| ai_provider_config | active provider, enabled |
| ai_settings | per-provider JSON |
| settings | general KV |
| audit_logs | all changes |

### 9.2 REST (optional V1.5)

```
GET/PATCH /api/v1/admin/settings/ai
GET/PATCH /api/v1/admin/settings/chat
GET/PATCH /api/v1/admin/settings/crm
```

---

## 10. Test Case

| TC ID | Scenario |
|-------|----------|
| TC-CFG-01 | ai.php load keys masked |
| TC-CFG-02 | super save provider |
| TC-CFG-03 | admin edit key → 403 |
| TC-CFG-04 | test connection ok |
| TC-CFG-05 | CRM rule save + audit |
| TC-CFG-06 | invalid temperature 3.0 |

---

## 11. Future

- Settings versioning + rollback
- Environment-specific overrides (staging/prod)
- Feature flags service

---

## 12. 부록 — ai.php·KV·보안

### 12.1 ai_provider_config singleton

active_provider: auto|anthropic|openai|gemini|grok|deepseek; enabled kill switch.

### 12.2 save_provider UPSERT

api_key empty = keep existing; log_activity on change.

### 12.3 Failover settings

consecutive_failures=2, timeout_sec=30, log_retention_days=90.

### 12.4 CRM settings UI (/admin/settings/crm.php)

auto_save, min_duration, schedule days, email templates per category_ai.

### 12.5 Notification detail

ADMIN_NOTIFY_EMAIL, MAIL_FROM from config/app.php; crm.save_failed → email + bell.

### 12.6 settings KV table

setting_key, setting_value, updated_by → audit on PATCH.

### 12.7 Extended tests TC-CFG-07..12

test_all providers, maintenance mode banner, prompt activate cascade.

### 12.8 ROLES legacy map

config/app.php ROLES super|admin|manager|sales|viewer vs ACEP admin console roles.

### 12.9 Security

AES keys at rest, CSRF all POST, rate limit test 5/min.

---

---

## 12. 설정 UI·모듈 상세 (Archive 통합)

### 12.1 UIUX — AI·Prompt·Failover·System

— AI 모델·파라미터

**경로**: `/admin/settings/ai.php` (**기존 참조 구현**)  
**역할**: super (full), admin (read + non-secret edit)

#### 6.5.1 목적

기존 `ai.php` 동작을 문서화하고 ACEP 필드 확장점을 정의. **구현과 모순 금지.**

#### 6.5.2 ASCII Layout (기존 정합)

```
┌─────────────────────────────────────────────────────────────────────────────┐
│  AI 설정                                                                     │
├─────────────────────────────────────────────────────────────────────────────┤
│  ■ Primary Model                                                             │
│    Provider      [OpenAI ▼]                                                  │
│    Model         [gpt-4o_______________]                                     │
│    API Key       [sk-••••••••••••3kFa]  [Reveal] (super only)               │
│  ■ Fallback Model                                                            │
│    Provider      [Anthropic ▼]                                               │
│    Model         [claude-3-5-sonnet________]                                 │
│    API Key       [sk-••••••••••••7xQ2]  [Reveal]                            │
│  ■ Parameters                                                                │
│    Temperature   [0.7____] (0.0 - 2.0)                                       │
│    Max Tokens    [2048___]                                                   │
│    Timeout (sec) [30_____]                                                   │
│  ■ Feature Flags                                                             │
│    [x] AI 추천 답변 활성화                                                    │
│    [x] 자동 감정 분석                                                         │
│    [ ] AI 자동 응답 (V2.0 — 비활성)                                          │
│  ─────────────────────────────────────────────────────────────────────────  │
│                              [테스트 연결]  [저장]                           │
└─────────────────────────────────────────────────────────────────────────────┘
```

#### 6.5.3 기존 ai.php 정합 규칙

| 필드 | 저장 위치 | 비고 |
|------|-----------|------|
| Provider/Model | `settings` KV 또는 `ai_config` JSON | 기존 로직 유지 |
| API Key | encrypted column | super only edit |
| Temperature | float 0-2 | validation 동일 |
| Test Connection | AJAX POST | OpenAI/Anthropic ping |

#### 6.5.4 ACEP 확장 (STEP 6 additive)

- Failover threshold (연속 실패 N회 → fallback)
- Recommendation display mode (inline / sidebar)
- Log retention days (ai_logs purge policy) — super only

---

### 6.6 Prompt Management — 프롬프트 관리

**경로**: `/admin/settings/prompts.php`  
**역할**: super, admin (admin: prod delete 불가)

#### 6.6.1 ASCII Layout — List

```
┌─────────────────────────────────────────────────────────────────────────────┐
│  프롬프트 관리                                    [+ 새 프롬프트] (super+)   │
├─────────────────────────────────────────────────────────────────────────────┤
│  [검색________] [타입 ▼: system|greeting|summary|reply|analyze] [활성 ▼]   │
├──────────┬─────────────┬─────────┬─────────┬──────────┬─────────────────────┤
│ Key      │ 이름         │ 버전    │ 타입    │ 수정일    │ Actions             │
├──────────┼─────────────┼─────────┼─────────┼──────────┼─────────────────────┤
│ sys_v3   │ System Base  │ v3      │ system  │ 07-20    │[편집][미리보기][↓]  │
│ greet_v2 │ 인사말       │ v2      │ greeting│ 07-18    │[편집][미리보기]     │
└──────────┴─────────────┴─────────┴─────────┴──────────┴─────────────────────┘
```

#### 6.6.2 ASCII Layout — Editor

```
┌─────────────────────────────────────────────────────────────────────────────┐
│  프롬프트 편집: sys_v3                              [버전 히스토리] [활성화]  │
├─────────────────────────────────────────────────────────────────────────────┤
│  Key *         [sys_v3______________] (immutable after create)               │
│  이름 *        [System Base Prompt___]                                       │
│  타입 *        [system ▼]                                                    │
│  ── Template ──────────────────────────────────────────────────────────────  │
│  │ You are PlusTok AI assistant for LG U+ ...                               │
│  │ Variables: {{customer_name}}, {{product_line}}, {{context}}              │
│  │                                                     [변수 삽입 ▼]        │
│  └──────────────────────────────────────────────────────────────────────────│
│  ── Preview (sample context) ─────────────────────────────────────────────  │
│  │ Rendered output preview...                                                │
│  └──────────────────────────────────────────────────────────────────────────│
│                              [저장 초안]  [배포(activate)]  [취소]           │
└─────────────────────────────────────────────────────────────────────────────┘
```

#### 6.6.3 DB: `ai_prompts`

| Column | Type | Note |
|--------|------|------|
| id | UUID | PK |
| prompt_key | VARCHAR | unique |
| version | INT | increment on save |
| template | TEXT | Handlebars-style vars |
| is_active | BOOL | one active per key |
| created_by | FK agents | audit |

---

### 6.7 Failover Log Viewer

**경로**: `/admin/settings/failover.php`  
**역할**: super, admin, operator (read)

#### 6.7.1 ASCII Layout

```
┌─────────────────────────────────────────────────────────────────────────────┐
│  AI Failover 로그                              [기간 ▼] [Provider ▼] [내보내기]│
├─────────────────────────────────────────────────────────────────────────────┤
│  ⚠ 지난 24시간 Failover 14건 · Primary 실패율 2.3%                          │
├──────────┬──────────┬─────────────┬─────────────┬──────────┬────────────────┤
│ 시각     │ Room ID  │ From → To   │ 사유        │ Latency  │ Detail         │
├──────────┼──────────┼─────────────┼─────────────┼──────────┼────────────────┤
│ 14:32:01 │ CR-1024  │ GPT→Claude  │ timeout 30s │ 31.2s    │ [펼치기]       │
│ 14:28:44 │ CR-1019  │ GPT→Claude  │ rate_limit  │ 0.8s     │ [펼치기]       │
└──────────┴──────────┴─────────────┴─────────────┴──────────┴────────────────┘
```

#### 6.7.2 Detail Expand

```
  Request ID: req_abc123
  Error: OpenAI API timeout after 30000ms
  Retry count: 1
  Fallback model: claude-3-5-sonnet
  ai_log_id: log_xyz789 → [ai_logs 상세 링크]
```

#### 6.7.3 DB: `ai_failover_log`

---

### 6.8 System Settings — 시스템 설정

**경로**: `/admin/settings/general.php`, `/admin/settings/audit.php`  
**역할**: super (full), admin (general subset)

#### 6.8.1 General Settings ASCII

```
┌─────────────────────────────────────────────────────────────────────────────┐
│  일반 설정                                                                   │
├─────────────────────────────────────────────────────────────────────────────┤
│  서비스명           [PlusTok ACEP___________]                                │
│  SLA 목표 응답(초)   [180___]                                                 │
│  상담 자동종료(분)   [30____]  (무응답)                                       │
│  WebSocket URL      [wss://ws.plustok.kr____] (super)                        │
│  Maintenance Mode   [ ] 활성  (super only — 전체 서비스 점검 배너)           │
│                              [저장]                                          │
└─────────────────────────────────────────────────────────────────────────────┘
```

#### 6.8.2 Audit Log Viewer ASCII

```
┌─────────────────────────────────────────────────────────────────────────────┐
│  감사 로그                     [기간] [Actor ▼] [Action ▼] [Resource ▼]       │
├──────────┬──────────┬─────────────┬──────────────────────┬──────────────────┤
│ 시각     │ Actor    │ Action      │ Resource             │ IP               │
├──────────┼──────────┼─────────────┼──────────────────────┼──────────────────┤
│ 14:00:01 │ admin01  │ prompt.update│ ai_prompts/sys_v3   │ 203.0.113.1      │
│ 13:55:22 │ super01  │ ai_key.update│ settings/ai          │ 203.0.113.2      │
└──────────┴──────────┴─────────────┴──────────────────────┴──────────────────┘
```

#### 6.8.3 DB: `audit_logs`

---

### 12.2 PHP Module — ai.php

### 6.1 Reference Implementation

> **중요**: 아래는 기존 `admin/settings/ai.php` 동작 문서화. 구현 변경 시 본 문서 동기화 필수.

**File**: `admin/settings/ai.php`

### 6.2 Request Flow

```
GET  ai.php
  → require_role(['super', 'admin'])
  → load settings from DB (key: ai_config JSON)
  → mask API keys unless role=super AND ?reveal=1
  → render form

POST ai.php
  → validate CSRF
  → validate fields (temperature 0-2, max_tokens 256-8192)
  → if role!=super: strip api_key fields from payload
  → encrypt api keys (AES-256-GCM)
  → save settings
  → activity_log('ai_settings.update', ...)
  → audit_logs INSERT (see §12)
  → redirect with flash success
```

### 6.3 Form Fields (Existing)

| Field | Name | Validation | super | admin |
|-------|------|------------|:-----:|:-----:|
| Primary Provider | `primary_provider` | enum | edit | edit |
| Primary Model | `primary_model` | string | edit | edit |
| Primary API Key | `primary_api_key` | string | edit | hidden/mask |
| Fallback Provider | `fallback_provider` | enum | edit | edit |
| Fallback Model | `fallback_model` | string | edit | edit |
| Fallback API Key | `fallback_api_key` | string | edit | hidden/mask |
| Temperature | `temperature` | float 0-2 | edit | edit |
| Max Tokens | `max_tokens` | int | edit | edit |
| Timeout | `timeout_sec` | int 5-120 | edit | edit |
| AI Recommend Enabled | `ai_recommend_enabled` | bool | edit | edit |
| Sentiment Analysis | `sentiment_enabled` | bool | edit | edit |

### 6.4 Test Connection AJAX

```php
// admin/settings/ai_test.php
require_role(['super', 'admin']);

$provider = $_POST['provider'];
$model = $_POST['model'];
$key = resolve_api_key($_POST, $_SESSION['role']); // super: new key; admin: DB key

$result = AiConnectionTester::ping($provider, $model, $key);

echo json_encode([
    'success' => $result->ok,
    'latency_ms' => $result->latency_ms,
    'message' => $result->message,
]);
```

### 6.5 ACEP Additive Fields (STEP 6)

```php
// Extend ai_config JSON — backward compatible
[
  // ... existing fields ...
  'failover_threshold' => 3,           // consecutive failures
  'recommendation_display' => 'inline', // inline|sidebar
  'ai_log_retention_days' => 90,       // super only
]
```

---

### 12.3 AI Settings Test Cases (UIUX §10.6)

| TC ID | 시나리오 | Expected |
|-------|----------|----------|
| TC-ADM-I01 | Load existing ai.php | Fields match DB, keys masked |
| TC-ADM-I02 | super save temperature | Success toast + audit |
| TC-ADM-I03 | Test connection success | Green check message |
| TC-ADM-I04 | Test connection fail | Red error with provider message |
| TC-ADM-I05 | Invalid temperature 3.0 | Client+server validation |

### 12.4 Prompt Test Cases (UIUX §10.7)

| TC ID | 시나리오 | Expected |
|-------|----------|----------|
| TC-ADM-P01 | Create draft prompt | version=1, is_active=false |
| TC-ADM-P02 | Activate prompt | Previous active deactivated |
| TC-ADM-P03 | admin delete prod prompt | 403 |

### 12.5 config/app.php 연동

```php
define('APP_NAME', 'PlusTok 통합 CRM');
define('ADMIN_NOTIFY_EMAIL', 'adfull@naver.com');
define('MAIL_FROM', 'noreply@plustok.mycafe24.com');
const ROLES = ['super', 'admin', 'manager', 'sales', 'viewer'];
```

### 12.6 ai.php AJAX _action 목록

| _action | 설명 |
|---------|------|
| test | active/auto provider ping |
| test_provider | 단일 provider ping |
| test_all | 5 providers 순회 |
| set_active_provider | active_provider 즉시 변경 |
| save_provider | api_key/model/temperature 저장 |

---

*End of document*
