# 작업지시서: Frontend/Admin 통합 인증 시스템 — 구현 메모

**구현일:** 2026-07-22  
**SSOT 사용자 저장소:** `agents` 테이블 (`AcepUserManager` — `acep_users` 별도 테이블 없음)

---

## 구현 완료 (Phase 1–4)

| Phase | 파일 | 상태 |
|-------|------|------|
| 1 | `config/acep.users.php` | ✅ `AcepUserManager` → `agents` |
| 2 | `admin/index.php` | ✅ agents 우선 로그인 + JWT 세션 + legacy managers 폴�백 |
| 3 | `frontend/src/services/auth.service.ts` | ✅ `username`/`loginId` 병행, `getCurrentUser()` |
| 4 | `api/v1/router.php` + `api/v1/auth.php` | ✅ `/auth/login` username alias |

---

## 통합 흐름

```
agents 테이블
    ├── POST /api/v1/auth/login  → Frontend JWT
    └── POST /admin/             → PHP 세션 + acep_jwt + manager shim
```

**동일 계정:** `agents.login_id` = Frontend 아이디 = Admin 로그인 ID

---

## 세션 키 (Admin)

| Key | 용도 |
|-----|------|
| `$_SESSION['acep_user']` | 통합 public user |
| `$_SESSION['acep_jwt']` | Frontend 호환 JWT |
| `$_SESSION['manager']` | Legacy CRM 페이지 호환 |

---

## 테스트

- `tests/Feature/AuthApiTest.php` — `username` alias
- `frontend/src/test/AuthProvider.test.tsx` — Vitest pass

---

## 미구현 (선택)

- [ ] 비밀번호 변경 UI/API (`AcepUserManager::changePassword`)
- [ ] managers → agents 일괄 마이그레이션 스크립트
- [ ] Admin 전용 React 대시보드로 PHP CRM 완전 대체

---

원본 작업지시서: Cursor scratchpad `작업지시서_통합인증시스템.md`
